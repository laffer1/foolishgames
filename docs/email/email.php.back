<?php


	include "libmail.php";

	$EmailFrom = trim( $HTTP_GET_VARS['from'] );
	$EmailFromName = trim( $HTTP_GET_VARS['name'] );
	$EmailBody = trim( $HTTP_GET_VARS['body'] );

	$m= new Mail; // create the mail
	
	$m->From( $EmailFrom );
	$m->To( "luke@foolishgames.com" );
	$m->Subject( "FoolishGames.com Email Submission" );
	$m->Body( $EmailBody );	// set the body
	$m->Priority(3) ;
	
	$m->Send();	// send the mail
	echo "Mail was sent:<br><pre>", $m->Get(), "</pre>";

?>
